#!/bin/bash
# Auto-generate build-info.json before commit

set -e

# Run build script to update build-info.json
node scripts/update-build-time.js

# Add the updated build-info.json to staging
git add docs/build-info.json

exit 0
