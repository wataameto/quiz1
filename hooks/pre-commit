#!/bin/bash
# Update build-info.json before commit
# This ensures build-info.json is always included in the commit

node scripts/update-build-time.js

# Add updated build-info.json to the commit
git add docs/build-info.json

exit 0
